<div class="p-5">
        
    <!-- Buscador de productos -->
    <h1 class="text-xl text-gray-700"> Nuevo producto </h1>
    <input 
            #txtDescripcion
            (keyup)="buscarProductos(txtDescripcion.value)"
            class="p-2 border shadow rounded mt-2"
            placeholder="Buscar producto">
        
    <!-- Lista de productos -->
        <div class="w-full">
        <div *ngFor="let producto of productos" 
                class="animate__animated animate__fadeIn flex my-1 cursor-pointer hover:bg-blue-lightest rounded text-xs border shadow hover:bg-yellow-100"
                (click)="seleccionarProducto(producto)"
                >
            <div class="w-8 h-10 text-center py-2">
            <p class="text-xl p-0 text-grey-dark">•</p>
            </div>
            <div class="w-4/5 h-10 py-2 px-1 mt-2">
            <p class="hover:text-blue-dark">
                <span class="font-semibold"> {{ producto.codigo }} </span> -
                {{ producto.descripcion }}
            </p>
            </div>
        </div>
        </div>
</div>